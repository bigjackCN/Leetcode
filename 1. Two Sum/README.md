Can you come up with an algorithm that is less than O(n2) time complexity?

Use hashmap to record the (target - current number) and store the index

Notice: Python Dictionary return KeyError for non-exit Key
